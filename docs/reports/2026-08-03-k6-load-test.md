# BNI Natcon 2026 — k6 Load Test Report

> Date: 2026-08-03 · k6 v2.1.0 · API: Go/chi + PostgreSQL 17 (localhost, fresh `natcon_k6` DB)
> Script: `scripts/k6/load.js` · Duration: 2m42s · Raw metrics: `k6-summary.json`

## Scenarios (concurrent)

| Scenario | Executor | Load | What it does |
|---|---|---|---|
| `browse` | ramping-vus | 0 → 20 → 60 → 0 VUs | Attendee journey: `/me`, `/tenants`, `/seminars`, `/networking` with think-time |
| `scan` | constant-arrival-rate | 20 rps flat | Booth scanner: `POST /scans` with random member codes (200 seeded members) |
| `admin` | constant-vus | 3 VUs | Committee dashboard polling: overview / ranking / fill / activity |

## Results — all thresholds passed ✅

| Metric | Value |
|---|---|
| Checks | **17,702 / 17,702 (100%)** |
| HTTP failures | **0 / 12,746 (0.00%)** |
| Throughput | 78.6 req/s sustained |
| `http_req_duration` p95 | **2.11 ms** (p90 1.78 ms, med 0.84 ms) |
| `browse` p95 | 1.52 ms |
| `scan` p95 | 2.64 ms (avg 1.78 ms, 3,001 scans, 0 errors) |
| `admin` p95 | 1.69 ms |
| Peak VUs | 64 concurrent |

Thresholds: `http_req_failed < 1%`, global p95 < 300 ms / p99 < 800 ms,
per-scenario p95 caps, `checks > 99%`, `scan_errors < 1%` — all green.

Note: the single 4.5 s max sample is the `setup()` bulk seed request
(200 members × bcrypt), not a runtime request.

## Functional checks embedded in the load

Every response was also asserted: member identity on each scan, sponsors
sorted first on the passport payload, and 200s across all four dashboards
— 100% held under load.
