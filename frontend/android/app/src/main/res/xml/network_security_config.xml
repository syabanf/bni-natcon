<?xml version="1.0" encoding="utf-8"?>
<!--
  The app talks to the API over TLS. Cleartext HTTP is off everywhere except
  the two addresses that only ever mean "the API on the machine I am testing
  from": the emulator's alias for the host, and localhost itself.

  A LAN address (192.168.x.y) is deliberately NOT here. Add one temporarily
  if you need to test against a laptop from a real phone, and do not ship
  that build.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
